boost::system is header only since 1.69 and removed in 1.90

--- CMakeLists.txt~	2025-04-26 01:28:02.000000000 +0200
+++ CMakeLists.txt	2026-09-03 22:56:16.918355806 +0200
@@ -9,7 +9,7 @@
   add_compile_options(-Wall -Wextra -Wpedantic)
 endif()
 
-find_package(Boost REQUIRED COMPONENTS thread system filesystem)
+find_package(Boost REQUIRED COMPONENTS thread filesystem)
 
 set(CATKIN_DISABLED false CACHE BOOL "Disable the catkin build, useful if catkin is present but a build outside of ros is done")
 if(NOT CATKIN_DISABLED)
